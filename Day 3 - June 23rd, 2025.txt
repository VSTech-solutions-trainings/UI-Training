Day 3 - June 23rd, 2025 

1) JS behind the scenes in Async flow

variables - unique identifiers 

2) Data types ??

JS 2 types of data types 1) Primitive 2) non -primitive 

what is primitive ? 

Numbers, Strings, Boolean, Null, undefined, symbol, bigint

Other than these primtive data types are called as non primitive ( Objects)

Objects -- Arrays, functions, object literals, 


Numbers ---

var a = 1 // 1.2 //1.333333

Math.random() // 0 - 1 ( 1 exclusion)

Math.random().toFixed(4)

Math.random() * 10 --- Math.random() * n (n can be any number) ( 0 -n excluding n)

Math.round() -- Math.round(Math.random() * 10) //6.7777777 Logs out 7 

Math.floor(Math.random() * 10) // prints nearest down integer value

Math.ceil(Math.random() * 10) // prints nearest up integer value

we can check the data type of any variable using typeof operator

Strings --- always enclosed in single or double quotes 

var name = 'This is Mike, I want to learn JavaScript'

  var count = name.charAt(0) // T

--
 var name = 'This is Mike, I want to learn JavaScript'

  //length, charAt(index)
//includes

  var count = name.includes('Mike111')

replace, replaceAll, toLowerCase, toUpperCase, split(), split(""), split(" "), concat, subStr, name.substring(4, 8)

-----

//// Boolean, logical operators && ||, truthy values, falsy values, if if else, elseif, while , for, for in, for of



